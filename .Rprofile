# in .Rprofile of the website project
if (file.exists("~/.Rprofile")) {
  base::sys.source("~/.Rprofile", envir = environment())
}

options(blogdown.new_bundle = TRUE,
        blogdown.author = "Greg Dubrow",
        blogdown.ext = '.Rmd',
        blogdown.subdir	 = "post")
